/**
 * Se ejecuta después de cargar el entorno de Jest en cada test del frontend.
 * Registra matchers extra (toBeInTheDocument, etc.) de @testing-library/jest-dom.
 */
import '@testing-library/jest-dom';
